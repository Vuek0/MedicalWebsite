import { defineStore } from "pinia";

export const useUser = defineStore('currentStore', {
    state: () => ({
        obj: {
            
        }
    }),
    actions: {
        // setPage(page){
        //     this.currentPage = page;
        // }
    }
});